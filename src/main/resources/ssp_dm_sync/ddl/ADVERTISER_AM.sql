CREATE TABLE `ADVERTISER_AM`(
  `id` int,
  `name` string,
  `phone` string,
  `skype` string,
  `qq` string,
  `jobtitle` string,
  `proxyid` int,
  `uniqueid` string,
  `companyid` int)
STORED AS ORC;