CREATE TABLE `ssp_overall_events_dwi`(
  `repeats` int,
  `rowkey` string,
  `id` int,
  `publisherid` int,
  `subid` int,
  `offerid` int,
  `campaignid` int,
  `countryid` int,
  `carrierid` int,
  `devicetype` int,
  `useragent` string,
  `ipaddr` string,
  `clickid` string,
  `price` double,
  `reporttime` string,
  `createtime` string,
  `clicktime` string,
  `showtime` string,
  `requesttype` string,
  `pricemethod` int,
  `bidprice` double,
  `adtype` int,
  `issend` int,
  `reportprice` double,
  `sendprice` double,
  `s1` string,
  `s2` string,
  `gaid` string,
  `androidid` string,
  `idfa` string,
  `postback` string,
  `sendstatus` int,
  `sendtime` string,
  `sv` string,
  `imei` string,
  `imsi` string,
  `imageid` int,
  `affsub` int,
  `s3` string,
  `s4` string,
  `s5` string,
  `packagename` string,
  `domain` string,
  `respstatus` int,
  `winprice` double,
  `wintime` string,
  `appprice` double,
  `test` int,
  `ruleid` int,
  `smartid` int,
  `pricepercent` int,
  `apppercent` int,
  `salepercent` int,
  `appsalepercent` int,
  `reportip` string,
  `eventname` string,
  `eventvalue` int,
  `refer` string,
  `status` int,
  `city` string,
  `region` string,
  `uid` string,
  `times` int,
  `time` int,
  `isnew` int,
  `pbresp` string,
  `ratertype` int,
  `raterid` string,
  `recommender` int)
PARTITIONED BY (
  `repeated` string,
  `l_time` string,
  `b_date` string,
  `b_time` string)
STORED AS ORC;